{
	"tasks": {
		"build": "npm run build",
		"start": "deno run -A .deno-deploy/server.ts",
		"preview": "deno run -A .deno-deploy/server.ts"
	},
	"exclude": ["node_modules", "build", ".svelte-kit", ".deno-deploy", "static"]
}
