{
	"tasks": {
		"build": "npm run build",
		"start": "PORT=8000 deno run --allow-env --allow-read --allow-net build/index.js",
		"preview": "PORT=8000 deno run --allow-env --allow-read --allow-net build/index.js"
	},
	"exclude": ["node_modules", "build", ".svelte-kit", "static"]
}
